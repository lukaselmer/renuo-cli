require 'spec_helper'

describe Renuo::Cli do
  it 'has a version number' do
    expect(Renuo::Cli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
